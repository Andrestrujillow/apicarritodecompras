import React from 'react';
// Importamos el componente que contiene la lógica y visualización de productos
import ProductList from '../components/ProductList';

const Products = () => {
  // Este componente es una simple página que muestra la lista de productos
  // Se encarga de renderizar el componente ProductList
  return <ProductList />;
};

export default Products;
